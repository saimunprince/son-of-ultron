"""SYRAX: web bridge layer on top of the OpenManus agent engine.

The OpenManus core under ``app/`` is left untouched. Everything SYRAX-specific
(persona, event streaming, web-based human input, WebSocket server) lives here.
"""
