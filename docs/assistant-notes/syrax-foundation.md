<!-- Copied from the assistant's memory on 2026-09-26 so it survives an OS change. Facts only; keys and addresses removed. -->


On 2026-09-26 Prince gave a long "SYRAX — Son of Ultron" master prompt (self-understanding, self-improving AI, 12 phases) plus a "verification & release gate" spec (never push unverified code, GREEN = evidence). Commits 5b25c07, cae9cf0, bc5070e on main implement the foundation:

- `backend/syrax/journal.py`: SQLite WAL journal (tasks/events/checkpoints/verifications), transition table, `task.completed` refused without a `final`, recovery with reality checks (`verify_operation` only knows `str_replace_editor`; everything else UNCERTAIN), dedupe-keyed idempotent recovery, resume_context/mark_resumed.
- `backend/syrax/core.py`: the task runs in a process-level `Core`; WS sessions are observers. Closing the browser no longer cancels work. `SYRAX_AUTO_RESUME=1` resumes only RESUMABLE tasks.
- `backend/syrax/verify.py`: `python -m syrax.verify` release gate (pytest/tsc/eslint/node test/isolated next build/diff_scan). Use it before every push. `next build` must never run in `frontend/` while the live `next start` serves `.next`; the gate builds from a scratch copy.
- Phase 3 self-model shipped the same day: `backend/syrax/selfmodel.py` (SelfModel, SelfInspectTool), WS `self_model`, `GET /self`, `frontend/components/SelfPanel.tsx` (key S). Capability status is derived from `journal.tool_stats()`; never hard-code answers there.
- Same day, non-stop per Prince's instruction: Phase 5 autonomy (`syrax/autonomy.py`, objectives judged from journal evidence, AUTO toggle off by default), Phase 11 observer (LIVE/HISTORY/TODAY tabs, `HistoryView.tsx`, WS task_detail/replay), Phase 6 research (`syrax/research.py`: ddgs + DuckDuckGo lite fallback, knowledge table with confidence policy, tools research/know/learn). Then Phase 7 skills (`syrax/skills.py`, tests decide registration, `backend/skills/`), Phase 8 devloop (`syrax/devloop.py`: `release` tool = gate → commit or rollback; SYRAX_AUTOPUSH off by default). Then Phase 9 operations (`syrax/resources.py`, `Journal.maintain`), Phase 10 presentation (`syrax/presentation.py` engine + `present` tool, `frontend/components/Stage.tsx`; presentation events are journaled task-less with task_id in the payload). Then Phase 12 (`syrax/bench.py` benchmark suite feeding the performance gate, `syrax/experiments.py` experiment tool, mechanism-targeting objectives). Fonts are self-hosted in frontend/app/fonts (Google Fonts fetch was flaky). Each phase: gate GREEN, pushed. All 12 phases have a first real implementation as of 2026-09-26.
- Docs: `docs/SYSTEM_MAP.md`, `docs/system_map.json`, `docs/GAP_ANALYSIS.md`, `docs/EXECUTION_MODEL.md` — keep them factual, update after each phase.
- Tests: `backend/syrax/conftest.py` gives every test a tmp journal and fresh core. Suite was 43 → 173 backend tests.

- 2026-09-26 afternoon: first supervised real runs on the live instance (Pollinations openai-fast brain, no keys). Verified: cycles close objectives from evidence, economical briefs cut a verify from 5 calls to 2. Fixed: no auto-verify of mutating tools, no cycle overlap, BUSY ticks not journaled, bench scratch cleanup. Observed: the free brain sometimes ends with a generic assistant sentence; a Groq/Gemini key would help. Disk on the laptop is ~98% full (sir's data, not SYRAX); the resource gate skips cycles under 2 GB free.

- Later the same day: task-quality benchmark (`syrax/quality.py`, WS `quality_run`, 10 machine-checked cases; first live baseline 80% on Pollinations) and `compare_versions` (`syrax/versions.py`, worktrees + tests + bench per commit). Autonomy loop is ON on the live instance (sir toggled it). Also done: history.jsonl retired (journal-backed `recent()`), presentation feedback learning (dismiss button → per-kind quiet preference), `compare_brains` via quality suite with `BrainRouter.preferred`.

- 2026-09-26 late: sir provided a Gemini key; it is saved ONLY in backend/config/brains.json (gitignored, 0600) via brains_save — never write it into files, docs or memory. Gemini is now first in the brain order: task-quality 100% (10/10) in 128 s vs pollinations 80% in 547 s. Facts memory moved into the journal (memory.json migrated once). Chart kind added to the stage.

- Milestone 2026-09-26 evening: SYRAX changed its own code end to end — commit b69a0ae authored `SYRAX <syrax@localhost>` (prompt line in TOOLS_GUIDE) through `release` (gate GREEN), pushed by me after review. First attempt exposed two safety bugs, fixed in c33b6cd: a task owns only its own changes (release/rollback never touch pre-existing dirty files), and unreleased autonomous edits are rolled back when the task ends. Rule for me: never leave uncommitted work on disk while SYRAX may run `release`; commit or stash first. Later: SYRAX derives code-fix objectives itself (persistent failing quality case, traceback in backend/syrax); quality runs report the actual answering brain. Known: Gemini free tier rate-limits after heavy use; Groq gpt-oss-120b returns 413 because SYRAX's prompt (20+ tools + memory) is too large for its on-demand limit — prompt size is an open weakness.
- Disk: sir's laptop runs at ~98-100% full (Downloads/www); the gate needs ~1 GB free; the resource gate skips cycles under 2 GB.

**Why:** Prince vibe-codes and will not verify manually; the gate and the truthful journal are the contract.
**How to apply:** What remains (docs/GAP_ANALYSIS.md tail): model fine-tuning/training (nothing trains), richer presentation vocabulary (charts/3D), facts memory still flat memory.json. Live compare_brains pollinations vs ollama was NOT run (ollama 7B on CPU would thrash sir's 14 GB laptop); run it when sir is away. Gemini and Groq keys are configured in brains.json only (never in files/docs/memory). Groq retired llama-3.3-70b in 2026-09; default is now openai/gpt-oss-120b. Measured task quality: gemini 100% (128 s), groq 90% (460 s, slow/rate-limited), pollinations 80% (547 s). Voice STT now uses Groq Whisper. Backend suite ~248 tests, takes ~100 s; full gate ~2 min. Live instance restart: `systemctl --user restart syrax`. Always: baseline tests first, gate GREEN, diff review, then push. Sir's live instance (systemd, port 8765/3000) runs old code until restarted; smoke-test on `SYRAX_PORT=8766` with a tmp `SYRAX_JOURNAL_FILE`. See [[syrax-project]].
