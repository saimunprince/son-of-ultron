# Assistant notes

Copies of the Claude Code project memory for SYRAX, kept here so an OS reinstall does not lose them. To restore them on a new machine, copy these files into `~/.claude/projects/<project>/memory/` and add one line per file to that directory's `MEMORY.md`.
